(jbuild_version 1)

(library
 ((name        git)
  (public_name git)
  (libraries   (fpath cstruct fmt lwt uri logs astring hex lru rresult
                angstrom encore ocamlgraph ocplib-endian
                ocplib-endian.bigstring decompress digestif))))
