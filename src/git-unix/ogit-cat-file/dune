(executable
  (name        main)
  (public_name ogit-cat-file)
  (package     git-unix)
  (libraries   git-unix
               cmdliner
               mtime mtime.clock.os
               fmt.cli fmt.tty
               logs.cli logs.fmt))
