(jbuild_version 1)

(executable
  ((name        main)
   (package     git-unix)
   (public_name ogit-cat-file)
   (libraries   (git-unix
                 cmdliner
                 mtime mtime.clock.os
                 fmt.cli fmt.tty
                 logs.cli logs.fmt))))
