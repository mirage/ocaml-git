(jbuild_version 1)

(executable
  ((name        main)
   (package     git-unix)
   (public_name ogit)
   (libraries   (git-unix cmdliner mtime mtime.clock.os fmt.cli logs.cli
                 fmt.tty logs.fmt))))
