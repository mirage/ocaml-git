(jbuild_version 1)

(executable
 ((name      test)
  (libraries (test decompress git.ocaml fpath lwt))))

(alias
 ((name runtest)
  (deps (test.exe ../data/pack.idx ../data/pack.pack))
  (action (run ${exe:test.exe} -q --color=always))))
